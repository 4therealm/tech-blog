{{> navBar}}
{{!-- <div class="post">
  <h3>{{post.title}}</h3>
  <p>{{post.content}}</p>
  <p>{{format_date post.createdAt}}  {{format_time post.createdAt}}</p>
  <h4>Comments:</h4>
  {{#each post.comments}}
    <div class="comment">
      <p>{{comment_text}}</p>
      <p>{{user.username}}</p>
    </div>
  {{/each}}
</div> --}}
<div class="post">
  <h3>{{post.title}}</h3>
  <p>{{post.content}}</p>
  <h4>Add a Comment:</h4>
 <form action="/comments/new" method="post">
  <input type="hidden" name="user_id" value="{{req.session.userId}}">
  <input type="hidden" name="post_id" value="{{post.id}}">
  <textarea name="comment_text"></textarea>
  <button type="submit">Submit Comment</button>
</form>

  <h4>Comments:</h4>
  {{#each post.comments}}
    <div class="comment">
      <p>{{comment_text}}</p>
    </div>
  {{/each}}
</div>
